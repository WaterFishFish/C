#define _CRT_SECURE_NO_WARNINGS 1	
#include <stdio.h>

void sortstring(char a[][20])
{
	int i, j;
	char temp[20];
	for (i = 0; i < 7; i++) {
		for (j = i + 1; j <8; j++) {
			if (strcmp(a[j], a[i]) < 0) {
				strcpy(temp, a[i]);
				strcpy(a[i], a[j]);
				strcpy(a[j], temp);
			}
		}
	}
}
int main()
{
	char a[8][20] = { 0 };
	for (int i = 0; i < 8; i++)
		scanf("%s",a[i]);
	sortstring(a);
	for (int i = 0; i < 8; i++)
		puts(a[i]);
	
}
