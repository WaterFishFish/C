#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
main()
{
	int arr[100];
	int brr[100];
	
	for (int i = 0; i < 10; i++)
		scanf("%d", &arr[i]);
	for (int i = 0; i <9; i++)
		brr[i] = arr[i] + arr[i + 1];
	for (int i = 0; i <9; i++)
		printf("%4d", brr[i]);
}
