#include<math.h>
#include<stdio.h>
int main() 
{
	int i, n, m;
	printf("input a number:");
	scanf("%d", &m);
	n = sqrt(m);
	for (i = 2; i <= n; i++)
	{
		if (m % i == 0)
		{
			printf("不是素数!\n");
			return 0;
		}
        }
			printf("是素数！\n");

			return 0;
}
