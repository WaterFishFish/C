#include<stdio.h>
int GetMax(int a,int b);
int main ()
{
   int x,y,max;
   printf("Input a,b:");
   scanf("%d,%d",&x,&y);
   max=GetMax(x,y);
   printf("max = %d\n",max);
   return 0;
}
int GetMax(int m,int n)
{
   if(m>=n)
      return m;
   else
      return n;
}

